name: Custom_Docker_Run_Action

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: nekmo/telegram-upload:master
      env:
        NODE_ENV: development
      ports:
        - 80
      volumes:
        - ${{ github.workspace }}/docker/files:/files
      options: --cpus 1
    steps:
    - name: Write session to file
      env:
        TLG_SESSION: ${{ secrets.TLG_SESSION }}
        APP_SESSION: ${{ secrets.APP_SESSION }}
        TELEGRAM_UPLOAD_CONFIG_DIRECTORY: /root/.config
      run: |
        mkdir -p /root/.config
        echo "$TLG_SESSION" | base64 --decode > /root/.config/telegram-upload.session
        echo "$APP_SESSION" > /root/.config/telegram-upload.json
        cat /root/.config/telegram-upload.json
        ls -R /files
    - name: run docker
      run: |
        /usr/local/bin/python /app/telegram_upload/management.py upload --config=/root/.config/telegram-upload.json text.txt
    # /app:
    # telegram_upload
    
    # /app/telegram_upload:
    # __init__.py
    # _compat.py
    # client.py
    # config.py
    # exceptions.py
    # files.py
    # management.py
    # utils.py
    # video.py        
